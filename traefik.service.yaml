apiVersion: v1
kind: Service
metadata:
  name: traefik-service
  namespace: kube-public
spec:
  selector:
    app: traefik
  ports:
  - port: 30021
    targetPort: 80
    protocol: TCP
    name: http
  - port: 30042
    targetPort: 8080
    protocol: TCP
    name: admin